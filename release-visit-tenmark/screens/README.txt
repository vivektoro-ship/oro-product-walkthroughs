Exported Figma PNGs go here, named exactly as the keys in the SCREENS map in index.html (e.g. px-pickup.png).
Until a file lands, the walkthrough shows a striped "Not exported" placeholder with the path it looked for — that is intentional.
